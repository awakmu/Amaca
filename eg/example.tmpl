<!DOCTYPE html>

<head>
	<title>{{ print(title) }}</title>
</head>

<body>
	<h1>{{ print(body) }}</h1>
	{{
		for i = 10, 1, -1 do
			print('\n\t\t<p>Number ' .. i .. '</p>\n')
		end
	}}
</body>
</html>
